---
phase: 05-password-hint-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/_middleware.js
  - public/index.html
  - public/rsvp/index.html
autonomous: false
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
must_haves:
  truths:
    - "Visiting / shows the hint page immediately (200, no browser auth dialog)"
    - "Heading 'Psst — ein kleines Rätsel für dich' and all four riddle lines are present"
    - "Clicking 'Zur Einladung →' triggers the Basic Auth dialog for /rsvp/"
    - "Correct credentials at /rsvp/ show the RSVP page (form, stepper, submit intact)"
    - "Wrong credentials at /rsvp/ + cancel → user sees the hint page HTML (from 401 body)"
    - "401 responses on protected routes carry Cache-Control: no-store"
  artifacts:
    - path: "public/index.html"
      provides: "Public hint page — links /style.css, German riddle, button to /rsvp/"
      contains: "Psst"
    - path: "public/rsvp/index.html"
      provides: "RSVP page (moved from public/index.html) with absolute resource paths"
      contains: "/style.css"
    - path: "functions/_middleware.js"
      provides: "Middleware: skips auth for /, /index.html, /style.css; 401+HTML for all other unauthenticated routes"
      contains: "pathname === '/'"
  key_links:
    - from: "browser GET /"
      to: "public/index.html (hint page)"
      via: "middleware pathname exclusion → context.next()"
      pattern: "pathname === '/'"
    - from: "browser GET /style.css (unauthenticated)"
      to: "public/style.css"
      via: "middleware pathname exclusion → context.next()"
      pattern: "pathname === '/style.css'"
    - from: "hint page button"
      to: "/rsvp/ (triggers browser auth dialog)"
      via: "<a href=\"/rsvp/\">"
      pattern: "href=\"/rsvp/\""
    - from: "unauthenticated GET /rsvp/"
      to: "401 HTML body (hint page)"
      via: "unauthorizedResponse()"
      pattern: "status: 401"
    - from: "authenticated GET /rsvp/"
      to: "public/rsvp/index.html"
      via: "context.next()"
      pattern: "return next()"
---

<objective>
Restructure the site so guests immediately see a warm hint page at the root URL (no browser auth
dialog), then navigate to /rsvp/ where Basic Auth protects the RSVP form.

Three changes:
1. `functions/_middleware.js` — skip auth for `/`, `/index.html`, and `/style.css`; return 401+HTML
   for all other unauthenticated routes (covers AUTH-04: cancel at /rsvp/ shows hint page)
2. `public/rsvp/index.html` — NEW: current RSVP content with absolute paths to /style.css, /script.js
3. `public/index.html` — REPLACE: hint page that links /style.css, shows German riddle, has CTA button

style.css is made public so both pages share it without duplication. script.js stays protected
(only needed on /rsvp/ where user is already authenticated).
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-password-hint-page/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create public/rsvp/index.html (RSVP page at new path)</name>

  <read_first>
    - `public/index.html` — read the full file; this is the source to copy verbatim
    - `public/script.js` line 97 — confirm `fetch('/api/rsvp', ...)` uses an absolute path (it does; no change needed)
  </read_first>

  <files>public/rsvp/index.html</files>

  <action>
Create directory `public/rsvp/` and write `public/rsvp/index.html` as a copy of the current
`public/index.html` with exactly two path changes (relative → absolute):
- `href="style.css"` → `href="/style.css"`
- `src="script.js"` → `src="/script.js"`

Everything else stays byte-for-byte identical (form, stepper, German content, comments).

Full file content:
```html
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Save the Date - 5. Dezember</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">

    <section class="save-the-date">
      <p class="eyebrow">Save the Date</p>
      <h1>5. Dezember</h1>
      <p>„Moin, Ich werde 30. Aus irgendwelchen Gründen ist das etwas Großes. Also reicht meine Wohnung vermutlich nicht. Daher möchte ich sammeln mit wie vielen ich rechnen muss, um eine Location zu finden. Meine aktuelle Idee ist ein gutes Buffet und anschließend feiern. Ich update die Seite, wenn ich mehr weiß und versuche euch zu informieren. Aber ich verspreche nichts :)."</p>
      <p class="deadline">Bitte bis Ende Mai anmelden.</p>
    </section>

    <div class="divider" aria-hidden="true"></div>

    <section class="rsvp-section">
      <h2>Anmeldung</h2>

      <!-- Phase 3: set action="/api/rsvp" method="POST" -->
      <form id="rsvp-form" action="#" method="get" novalidate>

        <!-- Name field (required) -->
        <div class="field">
          <label for="name">Name</label>
          <input
            type="text"
            id="name"
            name="name"
            placeholder="Dein Name"
            required
            autocomplete="given-name"
          />
          <span id="name-error" class="error-msg" role="alert">Bitte gib deinen Namen ein.</span>
        </div>

        <!-- Contact method (optional) -->
        <div class="field">
          <label for="contact">Bevorzugte Kontaktmethode <span class="optional">(optional)</span></label>
          <select id="contact" name="contact">
            <option value="">Bitte wählen</option>
            <option value="whatsapp">WhatsApp</option>
            <option value="telegram">Telegram</option>
            <option value="signal">Signal</option>
            <option value="discord">Discord</option>
            <option value="matrix">Matrix</option>
            <option value="sms">SMS</option>
            <option value="threema">Threema</option>
          </select>
        </div>

        <!-- Attendee count stepper (required, pre-filled 1) -->
        <div class="field">
          <label for="attendee-count">Anzahl der Personen</label>
          <div class="stepper">
            <button type="button" id="btn-less" aria-label="Weniger" disabled>−</button>
            <output id="attendee-count" aria-live="polite">1</output>
            <button type="button" id="btn-more" aria-label="Mehr">+</button>
          </div>
          <input type="hidden" id="attendee-value" name="attendees" value="1" />
        </div>

        <button type="submit" class="btn-submit">Anmelden</button>

      </form>
    </section>

  </div>
  <script src="/script.js"></script>
</body>
</html>
```
  </action>

  <verify>
    <automated>
node -e "
const fs = require('fs');
const src = fs.readFileSync('public/rsvp/index.html', 'utf8');
const checks = [
  ['absolute style path', src.includes('href=\"/style.css\"')],
  ['absolute script path', src.includes('src=\"/script.js\"')],
  ['no relative style', !src.includes('href=\"style.css\"')],
  ['no relative script', !src.includes('src=\"script.js\"')],
  ['RSVP form present', src.includes('id=\"rsvp-form\"')],
  ['5. Dezember present', src.includes('5. Dezember')],
  ['stepper present', src.includes('btn-submit')],
];
let pass = true;
checks.forEach(([l, ok]) => { console.log((ok?'PASS':'FAIL')+': '+l); if(!ok) pass=false; });
process.exit(pass ? 0 : 1);
"
    </automated>
  </verify>

  <acceptance_criteria>
    - File `public/rsvp/index.html` exists
    - `grep 'href="/style.css"' public/rsvp/index.html` returns a match
    - `grep 'src="/script.js"' public/rsvp/index.html` returns a match
    - `grep 'href="style.css"' public/rsvp/index.html` returns no match (relative path eliminated)
    - `grep 'id="rsvp-form"' public/rsvp/index.html` returns a match
    - `grep '5. Dezember' public/rsvp/index.html` returns a match
  </acceptance_criteria>

  <done>
    `public/rsvp/index.html` exists with absolute resource paths. RSVP form and all German content
    are identical to the original `public/index.html`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace public/index.html with hint page</name>

  <read_first>
    - `public/index.html` — read before overwriting to confirm it is still the RSVP page (Task 1 must be done first)
    - `public/style.css` — confirm `.save-the-date h1`, `.save-the-date p`, `.container`, `.btn-submit` styles exist; the hint page reuses them
    - `.planning/phases/05-password-hint-page/05-CONTEXT.md` — D-02, D-05, D-09, D-12 for exact text and CTA
  </read_first>

  <files>public/index.html</files>

  <action>
Replace the entire content of `public/index.html` with the hint page. The RSVP content is now
at `public/rsvp/index.html` (Task 1) and must NOT be in this file.

The hint page loads `/style.css` via `<link>` (style.css is made public in Task 3 — no inline
duplication needed). A small `<style>` block handles only the CTA button which doesn't exist
in the shared stylesheet.

Full file content:
```html
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Psst</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    .btn-hint {
      display: inline-block;
      margin-top: 32px;
      font-family: var(--font);
      font-size: 16px;
      font-weight: 700;
      color: #FFFFFF;
      background-color: var(--accent);
      border-radius: var(--radius);
      padding: 12px 24px;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <section class="save-the-date">
      <h1>Psst — ein kleines Rätsel für dich</h1>
      <p>Ich komme einmal im Jahr.<br>
      Man bäckt mir einen Kuchen, zündet Kerzen an und singt für mich.<br>
      Wer eingeladen ist, kennt mein Datum —<br>
      und kennt damit auch das Passwort.</p>
      <a href="/rsvp/" class="btn-hint">Zur Einladung →</a>
    </section>
  </div>
</body>
</html>
```

Constraints:
- D-02: Riddle text is exact — four lines, exact punctuation, exact German characters
- D-05: Heading is exact — "Psst — ein kleines Rätsel für dich" (em-dash U+2014, not a hyphen)
- D-09: This is a 200 page — no WWW-Authenticate, no 401 status
- D-12: CTA text is "Zur Einladung →", links to "/rsvp/"
- /style.css provides: body reset, `.container` (max-width 600px), `.save-the-date h1` (56px/700),
  `.save-the-date p` (16px/1.6/text color), background color — no inline duplication needed
  </action>

  <verify>
    <automated>
node -e "
const fs = require('fs');
const src = fs.readFileSync('public/index.html', 'utf8');
const checks = [
  ['links /style.css', src.includes('href=\"/style.css\"')],
  ['heading exact', src.includes('Psst \u2014 ein kleines R\u00e4tsel f\u00fcr dich')],
  ['riddle line 1', src.includes('Ich komme einmal im Jahr.')],
  ['riddle line 2', src.includes('Man b\u00e4ckt mir einen Kuchen')],
  ['riddle line 3', src.includes('kennt mein Datum')],
  ['riddle line 4', src.includes('und kennt damit auch das Passwort.')],
  ['CTA button to /rsvp/', src.includes('href=\"/rsvp/\"')],
  ['CTA text', src.includes('Zur Einladung')],
  ['no RSVP form', !src.includes('id=\"rsvp-form\"')],
  ['no 5. Dezember', !src.includes('5. Dezember')],
  ['lang=de', src.includes('lang=\"de\"')],
];
let pass = true;
checks.forEach(([l, ok]) => { console.log((ok?'PASS':'FAIL')+': '+l); if(!ok) pass=false; });
process.exit(pass ? 0 : 1);
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep 'href="/style.css"' public/index.html` returns a match (loads shared stylesheet)
    - File contains exact string: `Psst — ein kleines Rätsel für dich` (em-dash U+2014)
    - File contains: `Ich komme einmal im Jahr.` and `und kennt damit auch das Passwort.`
    - `grep 'href="/rsvp/"' public/index.html` returns a match
    - `grep 'id="rsvp-form"' public/index.html` returns no match (RSVP content gone)
    - `grep '5. Dezember' public/index.html` returns no match (RSVP content gone)
    - The node verification script above exits 0 (all 11 checks pass)
  </acceptance_criteria>

  <done>
    `public/index.html` is the hint page. It loads `/style.css`, shows the German riddle under the
    existing `.save-the-date` styles, and has a "Zur Einladung →" button linking to `/rsvp/`.
    No RSVP form content remains in this file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update _middleware.js — public exclusions + 401 HTML for protected routes</name>

  <read_first>
    - `functions/_middleware.js` — read the full file before replacing it
    - `.planning/phases/05-password-hint-page/05-CONTEXT.md` — D-08, D-09, D-11 for auth logic decisions
  </read_first>

  <files>functions/_middleware.js</files>

  <action>
Replace the entire content of `functions/_middleware.js`.

Changes from current file:
1. Add `new URL(request.url)` pathname extraction at top of `onRequest()`
2. Skip auth (call `next()`) for `/`, `/index.html`, and `/style.css`
3. Replace `unauthorizedResponse()` to return the hint page HTML as the 401 body (inline CSS
   required here since the 401 body must be self-contained — the browser won't load /style.css
   when cancelling a 401 dialog from a protected route)

New file content:
```javascript
// functions/_middleware.js
// HTTP Basic Auth middleware for Cloudflare Pages Functions.
//
// Public (no auth): /, /index.html, /style.css
// All other routes require Basic Auth. Unauthenticated requests receive a 401
// with the hint page HTML inline (self-contained — browser won't sub-resource-load
// style.css on a 401 cancel from /rsvp/).

export async function onRequest(context) {
  const { request, env, next } = context;
  const url = new URL(request.url);

  // Public routes — serve without auth challenge.
  if (
    url.pathname === '/' ||
    url.pathname === '/index.html' ||
    url.pathname === '/style.css'
  ) {
    return next();
  }

  const authHeader = request.headers.get("Authorization");

  if (!authHeader || !authHeader.startsWith("Basic ")) {
    return unauthorizedResponse();
  }

  const base64Credentials = authHeader.slice("Basic ".length);
  const credentials = atob(base64Credentials);
  // Use indexOf + slice (not split) to handle colons in the password safely.
  const colonIndex = credentials.indexOf(":");
  const password = credentials.slice(colonIndex + 1);

  if (password !== env.SITE_PASSWORD) {
    return unauthorizedResponse();
  }

  return next();
}

function unauthorizedResponse() {
  const html = `<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Psst</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      font-size: 16px;
      line-height: 1.6;
      color: #2D1510;
      background-color: #FDF4E8;
      padding: 80px 16px;
    }
    .container { max-width: 600px; margin: 0 auto; }
    h1 { font-size: 24px; font-weight: 700; line-height: 1.25; color: #2D1510; margin-bottom: 24px; }
    p { font-size: 16px; color: #2D1510; }
    @media (min-width: 600px) { body { padding: 80px 32px; } }
  </style>
</head>
<body>
  <div class="container">
    <h1>Psst — ein kleines Rätsel für dich</h1>
    <p>Ich komme einmal im Jahr.<br>
    Man bäckt mir einen Kuchen, zündet Kerzen an und singt für mich.<br>
    Wer eingeladen ist, kennt mein Datum —<br>
    und kennt damit auch das Passwort.</p>
  </div>
</body>
</html>`;

  return new Response(html, {
    status: 401,
    headers: {
      "WWW-Authenticate": 'Basic realm="Birthday Site"',
      "Content-Type": "text/html; charset=utf-8",
      "Cache-Control": "no-store",
    },
  });
}
```

Note: The 401 body has no CTA button — when this HTML is shown, the user came from /rsvp/ and
already knows how to get there. The inline CSS is intentional and necessary: sub-resource loads
(like /style.css) are blocked when a browser shows a 401 response body after cancelling auth.
  </action>

  <verify>
    <automated>
node -e "
const fs = require('fs');
const src = fs.readFileSync('functions/_middleware.js', 'utf8');
const checks = [
  ['pathname check for /', src.includes(\"pathname === '/'\" )],
  ['pathname check for /index.html', src.includes(\"pathname === '/index.html'\" )],
  ['pathname check for /style.css', src.includes(\"pathname === '/style.css'\" )],
  ['return next() present', src.includes('return next()')],
  ['Content-Type: text/html', src.includes('text/html; charset=utf-8')],
  ['Cache-Control: no-store', src.includes('no-store')],
  ['WWW-Authenticate', src.includes('WWW-Authenticate')],
  ['status 401', src.includes('status: 401')],
  ['heading in HTML', src.includes('Psst \u2014 ein kleines R\u00e4tsel f\u00fcr dich')],
  ['riddle line 1', src.includes('Ich komme einmal im Jahr.')],
  ['riddle line 4', src.includes('und kennt damit auch das Passwort.')],
  ['bg color #FDF4E8', src.includes('#FDF4E8')],
  ['text color #2D1510', src.includes('#2D1510')],
  ['onRequest exported', src.includes('export async function onRequest')],
];
let pass = true;
checks.forEach(([l, ok]) => { console.log((ok?'PASS':'FAIL')+': '+l); if(!ok) pass=false; });
process.exit(pass ? 0 : 1);
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "pathname === '/'" functions/_middleware.js` returns a match
    - `grep "pathname === '/style.css'" functions/_middleware.js` returns a match
    - `grep "text/html; charset=utf-8" functions/_middleware.js` returns a match
    - `grep "no-store" functions/_middleware.js` returns a match
    - `grep "WWW-Authenticate" functions/_middleware.js` returns a match
    - File contains: `Psst — ein kleines Rätsel für dich` (em-dash U+2014)
    - File contains: `Ich komme einmal im Jahr.`
    - `grep "export async function onRequest" functions/_middleware.js` returns a match
    - The node verification script above exits 0 (all 14 checks pass)
  </acceptance_criteria>

  <done>
    `functions/_middleware.js` serves `/`, `/index.html`, `/style.css` without auth. All other
    unauthenticated routes get a 401 with inline-CSS hint page HTML. 14 checks pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verify — full flow in browser</name>

  <what-built>
    - `/` public hint page (200, no auth dialog, styled via /style.css)
    - `/rsvp/` Basic Auth protected RSVP page (moved from /)
    - Cancel at /rsvp/ auth dialog shows inline hint page HTML
  </what-built>

  <how-to-verify>
    1. Start dev server: `npx wrangler pages dev public --compatibility-date=2024-01-01`
       (check `wrangler.jsonc` if command differs)

    2. **Hint page (AUTH-03):** Open `http://localhost:8788/`.
       Expected: hint page loads immediately — no auth dialog.
       Warm cream background, heading "Psst — ein kleines Rätsel für dich",
       four riddle lines, "Zur Einladung →" button. Style should match main site.

    3. **CTA → auth flow:** Click "Zur Einladung →".
       Expected: browser shows Basic Auth dialog for `/rsvp/`.

    4. **Successful auth:** Enter the correct password.
       Expected: RSVP page loads at `/rsvp/` — Save the Date heading, German text, form with stepper.

    5. **Wrong creds (AUTH-04):** Open a private/incognito window. Go to `http://localhost:8788/rsvp/`.
       Enter wrong credentials (e.g., user: `x`, password: `wrong`), then click Cancel.
       Expected: hint page HTML shown (riddle text, cream background — from 401 body).

    6. **Correct creds unaffected:** In same private window, go to `/rsvp/` and enter correct password.
       Expected: RSVP page loads normally.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all six checks pass.
    Describe any unexpected behaviour.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge | `/`, `/index.html`, `/style.css` are publicly accessible |
| Browser → Edge | All other routes (`/rsvp/`, `/api/`, `/script.js`) require Basic Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01 | Information Disclosure | Public hint page at `/` | accept | Contains only the riddle — no credentials, PII, or internal paths |
| T-05-02 | Information Disclosure | Public `/style.css` | accept | Contains only visual styling — no secrets, no auth logic |
| T-05-03 | Information Disclosure | Cache-Control on 401s | mitigate | `Cache-Control: no-store` on every 401 response prevents CDN caching |
| T-05-04 | Spoofing | `WWW-Authenticate` on 401s | accept | Browser dialog preserved; no custom login form → no phishing surface |
| T-05-05 | Elevation of Privilege | Pathname exclusion list | accept | Only `/`, `/index.html`, `/style.css` bypass auth; all API routes and JS remain protected |
</threat_model>

<verification>
After Tasks 1–3 complete, run all three node verification scripts (one per task). All checks must pass.
Then complete Task 4 (human verify) to confirm the full browser flow.
</verification>

<success_criteria>
1. `public/rsvp/index.html` exists with `href="/style.css"` and `src="/script.js"` (absolute paths), RSVP form intact
2. `public/index.html` is the hint page — loads `/style.css`, shows riddle, has button to `/rsvp/`, no RSVP form
3. `functions/_middleware.js` — public exclusions for `/`, `/index.html`, `/style.css`; 401+inline HTML for all other unauthenticated routes; `Cache-Control: no-store` + `WWW-Authenticate` on every 401
4. Visiting `/` shows hint page immediately (no auth dialog)
5. `/style.css` loads unauthenticated (shared by both hint page and RSVP page)
6. Clicking "Zur Einladung →" triggers Basic Auth dialog for `/rsvp/`
7. Correct credentials at `/rsvp/` → RSVP form with full content intact
8. Cancel at `/rsvp/` auth dialog → hint page HTML visible
</success_criteria>

<output>
After completion, create `.planning/phases/05-password-hint-page/05-01-SUMMARY.md`
</output>
