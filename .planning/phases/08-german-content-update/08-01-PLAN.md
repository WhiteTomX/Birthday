---
phase: 08-german-content-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/rsvp/index.html
  - functions/_middleware.js
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "The hint page riddle is the 5-line joint-birthday poem (per UI-SPEC.md canonical copy)"
    - "The 401 inline riddle in _middleware.js matches the hint page riddle exactly"
    - "The save-the-date body on /rsvp/ reads as a joint Wir/uns celebration with no personal names"
    - "The .deadline line reads as a soft no-deadline ask (Je früher ihr euch anmeldet...)"
    - "No single-host first-person Ich voice remains in the modified body paragraphs"
    - "public/script.js is untouched — confirmation messages are unchanged"
  artifacts:
    - path: "public/index.html"
      provides: "Updated hint page with 5-line riddle"
      contains: "Dieses Jahr feiern wir ihn zu zweit."
    - path: "public/rsvp/index.html"
      provides: "Updated save-the-date with joint Wir voice and soft RSVP ask"
      contains: "Wir werden 30."
    - path: "functions/_middleware.js"
      provides: "Inline 401 riddle synced to hint page"
      contains: "Dieses Jahr feiern wir ihn zu zweit."
  key_links:
    - from: "public/index.html riddle"
      to: "functions/_middleware.js unauthorizedResponse() riddle"
      via: "manual sync — both must contain identical 5-line poem"
      pattern: "Dieses Jahr feiern wir ihn zu zweit"
---

<objective>
Update all user-visible German copy across three files to reflect a joint 30th birthday celebration.

Per D-01, D-02, D-03: the hint page riddle gains a "zu zweit" line at position 2 (after "singt für."), making it a 5-line poem. Per D-03, the 401 riddle in `functions/_middleware.js` must stay byte-for-byte identical to the hint page riddle. Per D-04 through D-07: the save-the-date body paragraphs on `/rsvp/index.html` are replaced entirely with the canonical "Wir werden 30" joint-voice copy from UI-SPEC.md, and the hard deadline is replaced with a soft no-date RSVP ask.

Purpose: Guests arriving at the hint page, mistyping their password, or reading the save-the-date now all see a consistent joint-birthday message in German informal voice.

Output: Three modified HTML/JS files. No structural, auth, or backend changes.
</objective>

<execution_context>
@C:/Users/WhiteTom/Projects/Birthday/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/WhiteTom/Projects/Birthday/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Users/WhiteTom/Projects/Birthday/.planning/ROADMAP.md
@C:/Users/WhiteTom/Projects/Birthday/.planning/phases/08-german-content-update/08-CONTEXT.md
@C:/Users/WhiteTom/Projects/Birthday/.planning/phases/08-german-content-update/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hint page riddle and 401 inline riddle (D-01, D-02, D-03)</name>
  <files>public/index.html, functions/_middleware.js</files>

  <read_first>
    - `public/index.html` — current 4-line riddle block (lines 13–16); the `<p>` element uses `<br>` separators
    - `functions/_middleware.js` — `unauthorizedResponse()` function (lines 55–87); the inline riddle `<p>` block is at lines 69–72
    - `.planning/phases/08-german-content-update/08-UI-SPEC.md` — "Hint Page Riddle" section and "401 Inline Riddle" section for the exact canonical 5-line poem
  </read_first>

  <action>
    In `public/index.html`: Replace the single `<p>` riddle block (currently "Ich komme einmal im Jahr.\n Man bäckt mir einen Kuchen, zündet Kerzen an und singt für mich.\n Wer eingeladen ist, kennt mein Datum —\n und kennt damit auch das Passwort.") with the canonical 5-line version from UI-SPEC.md using `<br>` line separators:

      Ich komme einmal im Jahr.<br>
      Man bäckt einen Kuchen, zündet Kerzen an und singt für.<br>
      Dieses Jahr feiern wir ihn zu zweit.<br>
      Wer das Datum kennt —<br>
      kennt damit auch das Passwort.

    Do not alter the surrounding `<section class="save-the-date">`, `<h1>`, container div, or the redirect script at the bottom of the file. Only the riddle `<p>` block changes.

    In `functions/_middleware.js`: Replace the inline riddle `<p>` block inside `unauthorizedResponse()` (currently lines 69–72, the 4-line version) with the identical 5-line poem above, using the same `<br>` separator format. The surrounding heading (`<h1>Falsches Passwort</h1>`), intro sentence (`Das war leider nicht richtig...`), and reload instruction (`<p class="deadline">Seite neu laden...`) are unchanged (per UI-SPEC.md "Error State" section — these are locked strings). No other lines in `_middleware.js` are touched.

    Both files must end up with the same 5-line poem text — character-for-character identical riddle content.
  </action>

  <verify>
    <automated>grep -c "Dieses Jahr feiern wir ihn zu zweit." "C:/Users/WhiteTom/Projects/Birthday/public/index.html"</automated>
    <automated>grep -c "Dieses Jahr feiern wir ihn zu zweit." "C:/Users/WhiteTom/Projects/Birthday/functions/_middleware.js"</automated>
    <automated>grep -c "singt für mich" "C:/Users/WhiteTom/Projects/Birthday/public/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - `public/index.html` contains exactly: `Dieses Jahr feiern wir ihn zu zweit.` (1 occurrence)
    - `functions/_middleware.js` contains exactly: `Dieses Jahr feiern wir ihn zu zweit.` (1 occurrence)
    - `public/index.html` does NOT contain: `singt für mich` (the old singular phrasing is gone)
    - `functions/_middleware.js` does NOT contain: `singt für mich` (the old singular phrasing is gone)
    - `functions/_middleware.js` still contains: `Falsches Passwort` (locked error heading unchanged)
    - `functions/_middleware.js` still contains: `Seite neu laden, um es erneut zu versuchen.` (locked reload instruction unchanged)
    - `public/index.html` still contains: `window.location.replace('/rsvp/');` (redirect script unchanged)
  </acceptance_criteria>

  <done>Both files contain the identical 5-line joint-birthday riddle. The hint page and the 401 error page show the same poem. No locked strings were modified.</done>
</task>

<task type="auto">
  <name>Task 2: Replace save-the-date body paragraphs with joint Wir voice (D-04, D-05, D-06, D-07)</name>
  <files>public/rsvp/index.html</files>

  <read_first>
    - `public/rsvp/index.html` — full file; the body `<p>` block currently sits between `<h1>5. Dezember</h1>` (line 14) and `<div class="divider">` (line 23); includes the old `.deadline` line
    - `.planning/phases/08-german-content-update/08-UI-SPEC.md` — "Save-the-Date Body Text" section for the exact canonical HTML replacement block
  </read_first>

  <action>
    In `public/rsvp/index.html`: Remove ALL `<p>` elements between `<h1>5. Dezember</h1>` and `<div class="divider">` — that is, lines 15–20 in the current file (5 `<p>` elements: "Moin", the single-host Ich paragraph, the update paragraph, the volunteer paragraph, the old deadline). Replace them with the following 5 elements (exact canonical copy from UI-SPEC.md):

      <p>Moin</p>
      <p>Wir werden 30. Aus irgendwelchen Gründen ist das etwas Großes — und wir feiern das zusammen. Mit euch.</p>
      <p>Die Location steht noch nicht fest, aber der Plan ist: gutes Buffet, und danach richtig feiern. Wir halten euch auf dem Laufenden, sobald wir mehr wissen.</p>
      <p>Wenn wir jemanden vergessen haben — einfach den Link weitergeben!</p>
      <p class="deadline">Je früher ihr euch anmeldet, desto besser — dann können wir besser planen.</p>

    These five elements replace all content between `<h1>5. Dezember</h1>` and `<div class="divider">`. Do not alter `<p class="eyebrow">Save the Date</p>`, `<h1>5. Dezember</h1>`, `<div class="divider">`, the RSVP form section, or any other content in the file.
  </action>

  <verify>
    <automated>grep -c "Wir werden 30." "C:/Users/WhiteTom/Projects/Birthday/public/rsvp/index.html"</automated>
    <automated>grep -c "Je früher ihr euch anmeldet" "C:/Users/WhiteTom/Projects/Birthday/public/rsvp/index.html"</automated>
    <automated>grep -c "Ich werde 30" "C:/Users/WhiteTom/Projects/Birthday/public/rsvp/index.html"</automated>
    <automated>grep -c "Bitte bis Ende Mai anmelden" "C:/Users/WhiteTom/Projects/Birthday/public/rsvp/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - `public/rsvp/index.html` contains: `Wir werden 30.` (1 occurrence — joint framing anchor)
    - `public/rsvp/index.html` contains: `Je früher ihr euch anmeldet, desto besser — dann können wir besser planen.` (exact soft RSVP ask)
    - `public/rsvp/index.html` contains: `Wenn wir jemanden vergessen haben — einfach den Link weitergeben!` (share-the-link sentiment)
    - `public/rsvp/index.html` contains: `gutes Buffet, und danach richtig feiern.` (buffet + party plan)
    - `public/rsvp/index.html` does NOT contain: `Ich werde 30` (old single-host phrasing gone)
    - `public/rsvp/index.html` does NOT contain: `Bitte bis Ende Mai anmelden` (old hard deadline gone)
    - `public/rsvp/index.html` does NOT contain: `Ich habe in den letzten Jahren` (old volunteer paragraph gone)
    - `public/rsvp/index.html` still contains: `<p class="eyebrow">Save the Date</p>` (unchanged)
    - `public/rsvp/index.html` still contains: `<h1>5. Dezember</h1>` (unchanged)
    - `public/rsvp/index.html` still contains: `<div class="divider"` (divider element unchanged)
    - `public/rsvp/index.html` still contains: `<h2>Anmeldung</h2>` (form section unchanged)
    - `public/script.js` is NOT modified (verify by checking git status — script.js must not appear as modified)
  </acceptance_criteria>

  <done>The save-the-date section reads as a joint celebration in informal German Wir/uns voice. The soft RSVP ask replaces the old hard deadline. No names appear. All form markup is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → HTML files | Claude writes static content; no user-supplied data enters these files |
| Browser → 401 response | The 401 body is hardcoded in `_middleware.js`; riddle text is author-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `public/index.html` riddle | accept | Static file served by Cloudflare Pages; no runtime generation; integrity verified by exact-string grep gates in acceptance criteria |
| T-08-02 | Tampering | `functions/_middleware.js` 401 body | accept | Inline hardcoded string; no external inputs; identical-riddle sync verified by grep gates |
| T-08-03 | Information Disclosure | 401 page reveals riddle | accept | Intentional by design — the riddle is the password hint mechanism; disclosing it is the feature |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this phase — pure HTML/JS file edits only |
</threat_model>

<verification>
After both tasks complete, verify end state:

1. `grep -n "Dieses Jahr feiern wir ihn zu zweit." public/index.html` — returns exactly 1 match
2. `grep -n "Dieses Jahr feiern wir ihn zu zweit." functions/_middleware.js` — returns exactly 1 match
3. `grep -n "Wir werden 30." public/rsvp/index.html` — returns exactly 1 match
4. `grep -n "Je früher ihr euch anmeldet" public/rsvp/index.html` — returns exactly 1 match
5. `grep -c "singt für mich" public/index.html` — returns 0 (old phrasing absent)
6. `grep -c "Ich werde 30" public/rsvp/index.html` — returns 0 (old phrasing absent)
7. `grep -c "Bitte bis Ende Mai" public/rsvp/index.html` — returns 0 (old deadline absent)
8. `git diff --name-only` — shows exactly 3 files: `public/index.html`, `public/rsvp/index.html`, `functions/_middleware.js`
9. `git diff --name-only` — does NOT include `public/script.js` or `public/style.css` (unchanged)
</verification>

<success_criteria>
- CONT-01 satisfied: site text on hint page, save-the-date, and 401 error page all reflect joint birthday framing
- The 5-line riddle is identical in `public/index.html` and `functions/_middleware.js`
- The save-the-date body on `/rsvp/index.html` uses "Wir werden 30" anchor, informal "ihr/euch" register, and soft "Je früher" RSVP ask with no personal names
- Exactly 3 files modified; `public/script.js` and `public/style.css` are untouched
- D-01, D-02, D-03, D-04, D-05, D-06, D-07 implemented per CONTEXT.md decisions
- D-08: `public/script.js` confirmation messages verified unchanged
</success_criteria>

## Artifacts this phase produces

New symbols/strings introduced by this phase (not pre-existing):

| Type | Value | File |
|------|-------|------|
| Text string | `Dieses Jahr feiern wir ihn zu zweit.` | `public/index.html`, `functions/_middleware.js` |
| Text string | `singt für.` (line 2 of riddle, replaces "singt für mich.") | `public/index.html`, `functions/_middleware.js` |
| Text string | `Wer das Datum kennt —` (replaces "Wer eingeladen ist, kennt mein Datum —") | `public/index.html`, `functions/_middleware.js` |
| Text string | `kennt damit auch das Passwort.` (replaces "und kennt damit auch das Passwort.") | `public/index.html`, `functions/_middleware.js` |
| HTML element | `<p>Wir werden 30. Aus irgendwelchen Gründen...</p>` | `public/rsvp/index.html` |
| HTML element | `<p>Die Location steht noch nicht fest...</p>` | `public/rsvp/index.html` |
| HTML element | `<p>Wenn wir jemanden vergessen haben...</p>` | `public/rsvp/index.html` |
| HTML element | `<p class="deadline">Je früher ihr euch anmeldet, desto besser — dann können wir besser planen.</p>` | `public/rsvp/index.html` |

No new CSS classes, JS functions, API routes, or D1 columns are introduced.

<output>
Create `.planning/phases/08-german-content-update/08-01-SUMMARY.md` when done.
</output>
